import 'package:flutter/material.dart';

class ChroniclerMenuBar extends StatelessWidget {
  const ChroniclerMenuBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 40);
  }
}
