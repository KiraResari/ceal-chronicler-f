import 'package:flutter/material.dart';

class TimeBar extends StatelessWidget {
  const TimeBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Time Bar goes Here");
  }
}
