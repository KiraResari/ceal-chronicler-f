import 'package:ceal_chronicler_f/get_it_context.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'GetIt context can be initialized',
    (WidgetTester tester) async {
      initializeGetItContext();
    },
  );
}
